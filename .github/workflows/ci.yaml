name: ci
on: pull_request
jobs:
  go-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Run linters
        uses: golangci/golangci-lint-action@v8
        with:
          version: latest
          args: --timeout=3m
  go-test:
    strategy:
      matrix:
        platform: [ubuntu-latest]
    runs-on: ${{ matrix.platform }}
    env:
      BATON_MIRO_ACCESS_TOKEN: ${{ secrets.BATON_MIRO_ACCESS_TOKEN }}
      BATON_MIRO_SCIM_ACCESS_TOKEN: ${{ secrets.BATON_MIRO_SCIM_ACCESS_TOKEN }}
    steps:
      - name: Checkout code
        if: success()
        uses: actions/checkout@v4
      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Run go tests
        run: (set -o pipefail && go test -v -covermode=atomic -json -race ./... | tee test.json)
      - name: Annotate go tests
        if: always()
        uses: guyarb/golang-test-annotations@v0.6.0
        with:
          test-results: test.json

  integration-test:
    runs-on: ubuntu-latest
    env:
      BATON_LOG_LEVEL: debug
      BATON_MIRO_ACCESS_TOKEN: ${{ secrets.BATON_MIRO_ACCESS_TOKEN }}
      BATON_MIRO_SCIM_ACCESS_TOKEN: ${{ secrets.BATON_MIRO_SCIM_ACCESS_TOKEN }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Build baton-miro
        run: go build -o baton-miro ./cmd/baton-miro
      - name: Validate capabilities
        if: env.BATON_MIRO_ACCESS_TOKEN != '' && env.BATON_MIRO_SCIM_ACCESS_TOKEN != ''
        run: |
          ./baton-miro \
            --miro-access-token "$BATON_MIRO_ACCESS_TOKEN" \
            --miro-scim-access-token "$BATON_MIRO_SCIM_ACCESS_TOKEN" \
          capabilities
      - name: Run basic sync test
        if: env.BATON_MIRO_ACCESS_TOKEN != '' && env.BATON_MIRO_SCIM_ACCESS_TOKEN != ''
        run: |
          ./baton-miro \
            --miro-access-token "$BATON_MIRO_ACCESS_TOKEN" \
            --miro-scim-access-token "$BATON_MIRO_SCIM_ACCESS_TOKEN"

          if [ ! -f "sync.c1z" ]; then
            echo "Error: File sync.c1z was not generated."
            exit 1
          fi
      - name: List Baton Commands
        run: ./baton-miro --help
